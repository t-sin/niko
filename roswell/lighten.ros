#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp (ql:quickload '(:niko) :silent t)
  )

(defpackage :ros.script.run.3735452268
  (:use :cl))
(in-package :ros.script.run.3735452268)

(defun main (&rest argv)
  (declare (ignorable argv))
  ;;; TODO: start a watching thread.
  (format t "~%   --- My burden is light.~%")
  (niko:start)
  (niko:watch-forever)
  (loop (sleep 1)))
;;; vim: set ft=lisp lisp:
